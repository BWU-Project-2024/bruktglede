export const StoreTag = ({title}) => {
    return (
       <span role="tag" aria-label="Store Name" class="bg-peach-default hover:bg-peach-darker p-1 px-4 rounded-2xl mx-1 text-xs md:text-base">{title}</span>
       
    )
}