import { LoginForm } from "../auth/LoginForm"

export const LoginPage = () => {
    return (
        <main>
            <h1 className="">
                Login page
            </h1>
            <LoginForm />
        </main>
    )
}